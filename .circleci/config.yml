version: 2
jobs:
  build:
    docker:
      - image: node:12.16.3

    working_directory: /src

    steps:
      - checkout

      - run: yarn install

      - save_cache:
          paths:
            - /src/node_modules
          key: v1-dependencies-{{ checksum "package.json" }}

  eslint:
    docker:
      - image: node:12.16.3

    steps:
      - checkout

      - restore_cache:
          keys:
            - v1-dependencies-{{ checksum "package.json" }}
            - v1-dependencies-

      - run: yarn eslint


workflows:
  version: 2
  fast-test:
    jobs:
      - build
      - eslint:
          requires:
            - build
